<%# <p style="color: green"><%= notice %z></p>

<%= render @offer %z>

<div>
  <%= link_to "Edit this offer", edit_offer_path(@offer) %z> |
  <%= link_to "Back to offers", offers_path %z>

  <%= button_to "Destroy this offer", @offer, method: :delete %z>
</div> %>


<p style="color: green"><%= notice %></p>

<h1 class="my-5 text-center"><%= @offer.title %></h1>
<div class="text-center">
              <%# <%= image_tag @offer.image, class:"card-img-top w-25" %z> %>
        <div class="mb-5 text-center my-4">
            <%= @offer.description %>
        </div>
</div>
<div class="d-flex justify-content-between">
  <div>
    <%= render 'postulations/counters' %>
  </div>
  <div>
    <%= render 'postulations/postulations' %>
  </div>
</div>



<div class="d-flex gap-2 my-5">
  <%= link_to "Volver a Ofertas", offers_path, class:"btn btn-dark" %>
  <% if user_signed_in? %> 
    <% if current_user.id == @offer.user_id %>
      <%= link_to "Editar esta Oferta", edit_offer_path(@offer), class:"btn btn-dark" %>
      <%= button_to "Eliminar", @offer, class:"btn btn-danger btn-sm", method: :delete %>
    <% end %>
  <% end %>
  </div>
